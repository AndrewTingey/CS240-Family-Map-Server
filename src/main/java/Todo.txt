to do
Handler Classes - compare to ListGamesHandler.java
        inherit all from Handler.java
        DONE:
            /person

            /clear
            /user/login
            /user/register
            /load
            /event
            /event/[eventID] - fix json return message "Success"
            /fill/[username]/{generations}
            /person/[personID]
        TODO:

check:
also check for code duplication on:
    Service classes
    DAO classes

can do:
set generate people such that children inherit fathers last name

notes:
    fillFromRegister() on FillService.47 and FillService.67 assumes all who are registering are a male